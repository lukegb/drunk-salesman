// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBJA1VExYYQJq62iQh0nuK3mStzpDZKfEM",
    authDomain: "drunken-salesman.firebaseapp.com",
    databaseURL: "https://drunken-salesman.firebaseio.com",
    projectId: "drunken-salesman",
    storageBucket: "drunken-salesman.appspot.com",
    messagingSenderId: "553138037935",
    appId: "1:553138037935:web:b1fca063be843bb0"
  },
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
